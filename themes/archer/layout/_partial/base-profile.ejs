<div class="profile">
    <img class="profile-avatar" alt="avatar" src= <%- url_for(theme.avatar) %> >
    <div class="profile-name"><%- theme.author || config.author %></div>
    <% if(theme.signature && theme.signature.length !== 0) {%>
    <div class="profile-signature">
        <%= theme.signature %>
    </div>
    <% } %>
    <% if(theme.social) { %>
    <div class="profile-social">
        <%- partial('base-social') %>
    </div>
    <% } %>
    <% if(theme.links) { %>
        <div class="friends">
            <div>LINKS</div>
            <% for (name in theme.links) { %>
                <% if(theme.links[name]) { %>
                    <span>
                        <a href= <%- theme.links[name] %> target="_black"><%- name %></a>
                    </span>
                <% } %>
            <% } %>
        </div>
    <% } %>
    <% if(theme.about.enable) { %>
        <div class="about-me">
            <a href = "<%- url_for("/about") %>" >ABOUT ME</a>
        </div>
    <% } %>
</div>